import XCTest
@testable import PoetryReaderDomain

final class PoetryReaderDomainTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PoetryReaderDomain().text, "Hello, World!")
    }
}
